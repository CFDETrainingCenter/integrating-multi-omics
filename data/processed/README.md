# Processed intermediates (regenerated locally)

This folder is empty in the shipped package on purpose.

After you download portal files into `data/source/` and run the notebooks (or smoke runner), Module 1 writes QC-preprocessed AnnData objects here (and later modules may add other intermediates). Do not commit large h5ad files to GitHub.
